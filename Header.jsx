let {
    AppCanvas,
    AppBar,
    Styles,
    RaisedButton,
    DatePicker,
    TextField
    } = MUI;
let { ThemeManager, LightRawTheme } = Styles;

Header = React.createClass({
  render() {
    return (
      <AppBar title="Jibber" />
    );
  }
})
